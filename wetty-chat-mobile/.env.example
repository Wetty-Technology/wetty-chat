# Production only: backend API base URL (e.g. https://api.example.com).
# In development the app uses the Vite proxy at /api → http://localhost:3000; no env needed.
# VITE_API_BASE_URL=

# Backend requires X-User-Id header (integer). The app sends a placeholder until auth is implemented.
